# Core — MLX inference and fine-tuning on Apple Silicon
mlx>=0.22.0
mlx-lm>=0.22.0

# Speech Pipeline — ASR
mlx-whisper

# Speech Pipeline — TTS (Kokoro, used by default)
# kokoro                    # Optional: pip install kokoro

# Speech Pipeline — Neural Audio Codec
snac
torch>=2.4.0
soundfile

# Speech Pipeline — WebSocket server + streaming
websockets>=12.0
aiohttp>=3.9.0
numpy>=1.24.0

# Speech Pipeline — VAD (Silero via ONNX)
onnxruntime

# Optional — multimodal (vision) support
# mlx-vlm>=0.1.0

# Optional — TurboQuant+ KV cache compression
# pip install git+https://github.com/TheTom/mlx.git@feature/turboquant-plus

# Optional — ANE bridge (CoreML conversion)
# coremltools>=8.0
# transformers>=4.46.0
